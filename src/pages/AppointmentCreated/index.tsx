import React from 'react';
import { View, Button } from 'react-native';

const AppointmentCreate: React.FC = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center' }}>AppointmentCreate</View>
  );
};

export default AppointmentCreate;
